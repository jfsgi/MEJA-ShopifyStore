[
  {
    "name": "theme_info",
    "theme_name": "MEJA — Scandi Light",
    "theme_version": "0.1.0",
    "theme_author": "MEJA Designs",
    "theme_documentation_url": "https://github.com/jfsgi/MEJA-ShopifyStore",
    "theme_support_url": "https://github.com/jfsgi/MEJA-ShopifyStore/issues"
  },
  {
    "name": "Brand",
    "settings": [
      {
        "type": "image_picker",
        "id": "favicon",
        "label": "Favicon (browser tab icon)",
        "info": "Square image, 32×32 recommended (PNG)."
      },
      {
        "type": "image_picker",
        "id": "social_image",
        "label": "Social share image",
        "info": "Shown when pages are shared on social/Google (1200×630 recommended). Per-page it falls back to the product or article image."
      },
      {
        "type": "color",
        "id": "color_brand",
        "label": "Brand accent",
        "default": "#5b6b73"
      },
      {
        "type": "color",
        "id": "color_accent",
        "label": "Secondary accent",
        "default": "#9bb0a3"
      },
      {
        "type": "color",
        "id": "color_ink",
        "label": "Text",
        "default": "#232826"
      }
    ]
  },
  {
    "name": "Integrations",
    "settings": [
      {
        "type": "url",
        "id": "render_service_url",
        "label": "4kGraphics render service URL",
        "info": "Host of the 4kGraphics server (/v1/render, /v1/buildplan). Used for server-side 4K renders."
      },
      {
        "type": "url",
        "id": "crm_api_url",
        "label": "MEJA-CRM API URL (live pricing)",
        "info": "Origin of the CRM pricing engine (e.g. https://crm.mejadesigns.com). The configurator loads its option model from GET /v1/options and the authoritative, signed price from POST /v1/price here — the same engine that quotes a job. While a price is in flight (and if the host is blank or unreachable) it shows the local ESTIMATE, then replaces it with the live price. Leave blank only for a static preview."
      },
      {
        "type": "url",
        "id": "ar_service_url",
        "label": "AR service URL (View in your room)",
        "info": "Host of the 4kGraphics AR endpoint (POST /v1/ar → glb/usdz/poster). Set once it exists to enable the 'View in your room' AR button on the configurator. Leave blank to hide it."
      },
      {
        "type": "header",
        "content": "The 4kGraphics engine browser.js is uploaded to theme assets and embedded on the configurator template."
      }
    ]
  },
  {
    "name": "Cart",
    "settings": [
      {
        "type": "number",
        "id": "free_shipping_threshold",
        "label": "Free shipping threshold",
        "info": "Order subtotal (in your store currency, e.g. 150) that unlocks free shipping. Shown as a progress bar in the cart drawer. Set 0 to hide.",
        "default": 0
      }
    ]
  }
]
